Look at DEVELOPMENT.md and find the next pending commit (first one with status "⬜ Pending").

1. Show me which commit is next (number, message, phase)
2. List all the tasks for that commit
3. Ask if I want to proceed with implementing it

If I say yes, implement all tasks for that single commit:
- Create or modify only the files listed for that commit
- Follow the exact implementation details from DEVELOPMENT.md
- After all tasks are done, update DEVELOPMENT.md: change "⬜ Pending" → "✅ Done" for that commit
- Stage the relevant files and create the commit with the exact message from DEVELOPMENT.md

One commit at a time — do not proceed to the next commit automatically.
